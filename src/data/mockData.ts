// Re-export seed data for backward compatibility
export {
  seedConsoles as initialConsoles,
  seedCategories as initialCategories,
  seedMenuItems as initialMenuItems,
  seedPricing as initialPricing,
  seedControllers as initialControllers,
  seedShiftReports as initialShiftReports,
  seedMaintenanceRecords as initialMaintenanceRecords,
  seedCustomers as initialCustomers,
  seedAuditLog as initialAuditLog,
} from "./mock/seedData"
